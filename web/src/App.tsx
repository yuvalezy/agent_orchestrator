import { type FormEvent, type ReactElement, type ReactNode, useEffect, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Activity, BarChart3, CheckCircle2, CircleAlert, ClipboardList, LogOut, Menu, Plug, RefreshCw, Send, ShieldCheck, SlidersHorizontal, Users } from 'lucide-react';
import { api, type ApiError, setCsrfToken } from './lib/api';
import { cn } from './lib/utils';
import { ApprovalsView } from './ApprovalsView';
import { SettingsView } from './SettingsView';
import { ConnectorsView } from './ConnectorsView';

type Overview = {
  data: {
    status: 'ok' | 'degraded'; db: 'ok' | 'down'; uptime: number;
    backlog: { inbox: Bucket; outboundQueue: Bucket }; workers: Worker[];
    queueStates: { inbox: QueueState[]; outbound: QueueState[] };
    activeChannels: Channel[]; featureFlags: FeatureFlag[]; capabilities: Capability[];
  };
};
type Bucket = { pending: number; failed: number; oldestPendingAgeSeconds: number | null };
type QueueState = { status: string; count: number };
type Channel = { name: string; channelType: string; provider: string };
type FeatureFlag = { name: string; enabled: boolean };
type Capability = { name: string; available: boolean; detail: string };
type WorkerState = 'registered_idle' | 'working' | 'healthy' | 'stale' | 'failing_backoff' | 'not_registered';
type WorkerRegistration = 'registered' | 'flag_off' | 'not_registered';
type Worker = { name: string; intervalMs: number; lastRunAt: string | null; lastSuccessAt: string | null; lastError: string | null; consecutiveFailures: number; isRunning: boolean; state: WorkerState; registration: WorkerRegistration };
type Row = Record<string, unknown>;
type Page = { data: Row[]; nextCursor: string | null };
type Insights = { data: { rangeDays: number; llm: { calls: number; inputTokens: number; outputTokens: number; totalUsd: number; lastCallAt: string | null; byProviderRole: Array<{ provider: string; role: string; calls: number; totalUsd: number }> }; knowledge: { activeDocuments: number; tombstonedDocuments: number; activeChunks: number; lastSyncedAt: string | null; internalChunks: number; lastInternalUpdateAt: string | null }; taskInventory: { activeDocuments: number; customers: number; lastSyncedAt: string | null; portalUrl: string | null }; releaseNotes: { notificationsInRange: number; totalNotifications: number; lastProcessedAt: string | null } } };

const nav = [
  ['overview', 'Overview', Activity],
  ['approvals', 'Pending actions', CheckCircle2],
  ['workers', 'Worker health', Activity],
  ['inbox', 'Inbox', ClipboardList],
  ['outbound', 'Outbound', Send],
  ['customers', 'Conversations', Users],
  ['decisions', 'Decisions', ShieldCheck],
  ['insights', 'Insights', BarChart3],
  ['settings', 'Settings', SlidersHorizontal],
  ['connectors', 'Connectors', Plug],
] as const;
type View = typeof nav[number][0];

const fmt = new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' });
function displayDate(value: unknown): string { return typeof value === 'string' ? fmt.format(new Date(value)) : '—'; }
function display(value: unknown): string { return typeof value === 'string' && value ? value : '—'; }
function usd(value: number): string { return new Intl.NumberFormat(undefined, { style: 'currency', currency: 'USD', maximumFractionDigits: 4 }).format(value); }

function readConsoleParam(key: string, fallback = ''): string { return new URLSearchParams(window.location.search).get(key) ?? fallback; }
function writeConsoleParam(key: string, value: string): void { const url = new URL(window.location.href); if (value) url.searchParams.set(key, value); else url.searchParams.delete(key); window.history.replaceState({}, '', url); window.dispatchEvent(new Event('console:urlchange')); }
function useConsoleUrlParam(key: string, fallback = ''): readonly [string, (value: string) => void] { const [value, setValue] = useState(() => readConsoleParam(key, fallback)); useEffect(() => { const sync = () => setValue(readConsoleParam(key, fallback)); window.addEventListener('popstate', sync); window.addEventListener('console:urlchange', sync); return () => { window.removeEventListener('popstate', sync); window.removeEventListener('console:urlchange', sync); }; }, [key, fallback]); return [value, (next) => writeConsoleParam(key, next)] as const; }
function useConsoleUrlOptionalParam(key: string): readonly [string | null, (value: string | null) => void] { const [value, setValue] = useState<string | null>(() => readConsoleParam(key) || null); useEffect(() => { const sync = () => setValue(readConsoleParam(key) || null); window.addEventListener('popstate', sync); window.addEventListener('console:urlchange', sync); return () => { window.removeEventListener('popstate', sync); window.removeEventListener('console:urlchange', sync); }; }, [key]); return [value, (next) => writeConsoleParam(key, next ?? '')] as const; }

export function App(): ReactElement {
  const [authenticated, setAuthenticated] = useState<boolean | null>(null);
  const queryClient = useQueryClient();
  const session = useQuery({ queryKey: ['session'], queryFn: () => api<{ data: { csrfToken: string } }>('/session'), retry: false });
  useEffect(() => {
    if (authenticated !== null) return;
    if (session.isSuccess) { setCsrfToken(session.data.data.csrfToken); setAuthenticated(true); }
    if (session.isError) setAuthenticated(false);
  }, [authenticated, session.isError, session.isSuccess, session.data]);
  useEffect(() => {
    const clear = () => { setCsrfToken(null); queryClient.clear(); setAuthenticated(false); };
    window.addEventListener('console:unauthorized', clear);
    return () => window.removeEventListener('console:unauthorized', clear);
  }, [queryClient]);
  if (authenticated === null) return <div className="grid min-h-screen place-items-center text-zinc-400">Checking secure session…</div>;
  return authenticated ? <Console onLogout={() => { setCsrfToken(null); queryClient.clear(); setAuthenticated(false); }} /> : <Login onSuccess={(csrf) => { setCsrfToken(csrf); setAuthenticated(true); }} />;
}

function Login({ onSuccess }: { onSuccess: (csrf: string) => void }): ReactElement {
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const login = useMutation({
    mutationFn: () => api<{ data: { csrfToken: string } }>('/session', { method: 'POST', body: JSON.stringify({ password }) }),
    onSuccess: ({ data }) => onSuccess(data.csrfToken),
    onError: (err: ApiError) => setError(err.message),
  });
  const submit = (event: FormEvent) => { event.preventDefault(); setError(null); login.mutate(); };
  return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,_#27272a,_#09090b_52%)] p-5">
    <form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-zinc-800 bg-zinc-950/80 p-7 shadow-2xl shadow-black/30">
      <div className="mb-7 flex size-11 items-center justify-center rounded-xl bg-emerald-400 text-zinc-950"><ShieldCheck size={24} /></div>
      <h1 className="text-xl font-semibold">Founder console</h1>
      <p className="mt-2 text-sm leading-6 text-zinc-400">Private operations access for the agent orchestrator.</p>
      <label className="mt-7 block text-sm font-medium">Password<input autoFocus type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2.5 outline-none ring-emerald-400 focus:ring-2" /></label>
      {error && <p className="mt-3 text-sm text-red-300">{error}</p>}
      <button disabled={login.isPending || !password} className="mt-5 flex w-full items-center justify-center rounded-lg bg-emerald-400 px-4 py-2.5 text-sm font-semibold text-zinc-950 enabled:hover:bg-emerald-300 disabled:opacity-50">{login.isPending ? 'Signing in…' : 'Sign in securely'}</button>
      <p className="mt-5 text-center text-xs text-zinc-500">Tailnet access and app session required.</p>
    </form>
  </main>;
}

function Console({ onLogout }: { onLogout: () => void }): ReactElement {
  const [viewParam, setViewParam] = useConsoleUrlParam('view', 'overview');
  const view = nav.some(([key]) => key === viewParam) ? viewParam as View : 'overview';
  const [menuOpen, setMenuOpen] = useState(false);
  const client = useQueryClient();
  const logout = useMutation({ mutationFn: () => api<void>('/session', { method: 'DELETE' }), onSettled: onLogout });
  const choose = (next: View) => { setViewParam(next); setMenuOpen(false); };
  return <div className="min-h-screen bg-zinc-950 text-zinc-100">
    <aside className={cn('fixed inset-y-0 z-20 w-64 border-r border-zinc-800 bg-zinc-950 p-4 transition-transform md:translate-x-0', menuOpen ? 'translate-x-0' : '-translate-x-full')}>
      <div className="mb-7 flex items-center gap-3 px-2"><div className="grid size-8 place-items-center rounded-lg bg-emerald-400 text-zinc-950"><Activity size={18} /></div><span className="font-semibold">AO Console</span></div>
      <nav className="space-y-1">{nav.map(([key, label, Icon]) => <button key={key} onClick={() => choose(key)} className={cn('flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition', view === key ? 'bg-zinc-800 text-white' : 'text-zinc-400 hover:bg-zinc-900 hover:text-zinc-100')}><Icon size={17} />{label}</button>)}</nav>
      <button onClick={() => logout.mutate()} className="absolute bottom-5 flex items-center gap-3 px-3 text-sm text-zinc-400 hover:text-white"><LogOut size={17} />Sign out</button>
    </aside>
    {menuOpen && <button aria-label="Close navigation" onClick={() => setMenuOpen(false)} className="fixed inset-0 z-10 bg-black/60 md:hidden" />}
    <main className="min-h-screen md:ml-64">
      <header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-zinc-800 bg-zinc-950/85 px-5 backdrop-blur"><button onClick={() => setMenuOpen(true)} className="md:hidden"><Menu /></button><div className="text-sm text-zinc-400">Founder-only · Tailscale protected</div><button onClick={() => client.invalidateQueries()} className="rounded-md p-2 text-zinc-400 hover:bg-zinc-900 hover:text-white" title="Refresh"><RefreshCw size={17} /></button></header>
      <div className="mx-auto max-w-7xl p-5 md:p-8">{view === 'overview' && <OverviewView onSelect={choose} />}{view === 'approvals' && <ApprovalsView />}{view === 'workers' && <WorkersView />}{view === 'inbox' && <InboxView />}{view === 'outbound' && <OutboundView />}{view === 'customers' && <CustomersView />}{view === 'decisions' && <DecisionsView />}{view === 'insights' && <InsightsView />}{view === 'settings' && <SettingsView />}{view === 'connectors' && <ConnectorsView />}</div>
    </main>
  </div>;
}

function OverviewView({ onSelect }: { onSelect: (view: View) => void }): ReactElement {
  const overview = useQuery({ queryKey: ['overview'], queryFn: () => api<Overview>('/overview'), refetchInterval: 30_000 });
  if (overview.isLoading) return <Loading title="Loading operational state…" />;
  if (overview.isError) return <ErrorState message={(overview.error as Error).message} />;
  if (!overview.data) return <Loading title="Loading operational state…" />;
  const data = overview.data.data;
  return <section><PageTitle eyebrow="Live system" title="Operations overview" description="Bounded runtime state from the orchestrator database, worker registry, and active configuration." />
    <div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Database" value={data.db === 'ok' ? 'Connected' : 'Unavailable'} tone={data.db === 'ok' ? 'good' : 'bad'} /><Metric label="Inbox pending" value={String(data.backlog.inbox.pending)} detail={`${data.backlog.inbox.failed} failed`} onClick={() => onSelect('inbox')} /><Metric label="Outbound pending" value={String(data.backlog.outboundQueue.pending)} detail={`${data.backlog.outboundQueue.failed} failed`} /><Metric label="Workers" value={String(data.workers.length)} detail={`${data.workers.filter((w) => w.state === 'failing_backoff' || w.state === 'stale' || w.registration === 'not_registered').length} need attention`} onClick={() => onSelect('workers')} /></div>
    <div className="mt-7 grid gap-5 xl:grid-cols-2"><Panel title="Attention needed"><Attention workers={data.workers} /></Panel><Panel title="Capabilities"><div className="space-y-3">{data.capabilities.map((capability) => <div key={capability.name} className="flex items-start justify-between gap-3 text-sm"><div><p className="font-medium text-zinc-200">{capability.name}</p><p className="mt-1 text-xs text-zinc-500">{capability.detail}</p></div><StateBadge enabled={capability.available} offLabel="unavailable" /></div>)}</div></Panel><Panel title="Queue state"><QueueStates label="Inbox" rows={data.queueStates.inbox} /><QueueStates label="Outbound" rows={data.queueStates.outbound} /></Panel><Panel title="Active channel instances"><div className="space-y-3">{data.activeChannels.length === 0 && <p className="text-sm text-zinc-500">No active channel instances in the database.</p>}{data.activeChannels.map((channel) => <div key={channel.name} className="flex items-center justify-between gap-3 text-sm"><span className="font-mono text-xs text-zinc-200">{channel.name}</span><span className="text-xs text-zinc-500">{channel.channelType} · {channel.provider}</span></div>)}</div></Panel><Panel title="Feature switches"><div className="grid gap-3 sm:grid-cols-2">{data.featureFlags.map((flag) => <div key={flag.name} className="flex items-center justify-between gap-3 text-sm"><span className="text-zinc-300">{flag.name}</span><StateBadge enabled={flag.enabled} onLabel="enabled" /></div>)}</div></Panel></div>
  </section>;
}

function InsightsView(): ReactElement {
  const [daysParam, setDaysParam] = useConsoleUrlParam('insightsDays', '30'); const days = [7, 30, 90].includes(Number(daysParam)) ? Number(daysParam) : 30;
  const insights = useQuery({ queryKey: ['insights', days], queryFn: () => api<Insights>(`/insights?days=${days}`) });
  if (insights.isLoading) return <Loading title="Loading local operational summaries…" />;
  if (insights.isError) return <ErrorState message={(insights.error as Error).message} />;
  if (!insights.data) return <Loading title="Loading local operational summaries…" />;
  const data = insights.data.data;
  return <section><PageTitle eyebrow="Local aggregates" title="Insights" description="Bounded server-side aggregates from the orchestrator database. Task inventory is cached locally; release-note activity is the local notification ledger, not a live source read." /><div className="mt-6 flex gap-2">{[7, 30, 90].map((value) => <button key={value} onClick={() => setDaysParam(String(value))} className={cn('rounded-full border px-3 py-1.5 text-sm', days === value ? 'border-emerald-400 bg-emerald-400 text-zinc-950' : 'border-zinc-700 text-zinc-300')}>{value} days</button>)}</div><div className="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-4"><Panel title="LLM spend"><p className="text-2xl font-semibold">{usd(data.llm.totalUsd)}</p><p className="mt-2 text-sm text-zinc-400">{data.llm.calls} calls · {data.llm.inputTokens + data.llm.outputTokens} tokens</p><p className="mt-2 text-xs text-zinc-500">Last call: {displayDate(data.llm.lastCallAt)}</p></Panel><Panel title="Customer knowledge"><p className="text-2xl font-semibold">{data.knowledge.activeDocuments} docs</p><p className="mt-2 text-sm text-zinc-400">{data.knowledge.activeChunks} active chunks · {data.knowledge.tombstonedDocuments} tombstoned</p><p className="mt-2 text-xs text-zinc-500">Last sync: {displayDate(data.knowledge.lastSyncedAt)}</p></Panel><Panel title="Task inventory cache"><p className="text-2xl font-semibold">{data.taskInventory.activeDocuments} tasks</p><p className="mt-2 text-sm text-zinc-400">{data.taskInventory.customers} customers represented</p><p className="mt-2 text-xs text-zinc-500">Last local sync: {displayDate(data.taskInventory.lastSyncedAt)}</p>{data.taskInventory.portalUrl && <a href={data.taskInventory.portalUrl} target="_blank" rel="noreferrer" className="mt-3 inline-block text-xs font-medium text-emerald-300 hover:text-emerald-200">View tasks in portal</a>}</Panel><Panel title="Release-note ledger"><p className="text-2xl font-semibold">{data.releaseNotes.notificationsInRange} updates</p><p className="mt-2 text-sm text-zinc-400">{data.releaseNotes.totalNotifications} total local notification records</p><p className="mt-2 text-xs text-zinc-500">Last processed: {displayDate(data.releaseNotes.lastProcessedAt)}</p></Panel></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><Panel title="LLM cost by provider and role">{data.llm.byProviderRole.length === 0 ? <p className="text-sm text-zinc-500">No cost records in this date range.</p> : <div className="divide-y divide-zinc-800">{data.llm.byProviderRole.map((row) => <div key={`${row.provider}-${row.role}`} className="flex items-center justify-between gap-3 py-3 text-sm"><span>{row.provider} · {row.role} <span className="text-zinc-500">({row.calls} calls)</span></span><span className="font-medium">{usd(row.totalUsd)}</span></div>)}</div>}</Panel><Panel title="Internal knowledge"><p className="text-2xl font-semibold">{data.knowledge.internalChunks} chunks</p><p className="mt-2 text-xs text-zinc-500">Last update: {displayDate(data.knowledge.lastInternalUpdateAt)}</p></Panel></div></section>;
}

function WorkersView(): ReactElement {
  const workers = useQuery({ queryKey: ['workers'], queryFn: () => api<{ data: Worker[] }>('/workers'), refetchInterval: 30_000 });
  if (workers.isLoading) return <Loading title="Loading worker health…" />;
  if (workers.isError) return <ErrorState message={(workers.error as Error).message} />;
  if (!workers.data) return <Loading title="Loading worker health…" />;
  return <section><PageTitle eyebrow="Runtime" title="Worker health" description="Registered workers are classified by their live run state. Failure categories are safe; upstream error text is never exposed." /><div className="mt-7 overflow-hidden rounded-xl border border-zinc-800"><table className="w-full text-left text-sm"><thead className="bg-zinc-900 text-zinc-400"><tr><th className="p-4">Worker</th><th className="p-4">Last started</th><th className="p-4">Last success</th><th className="p-4">Cadence</th><th className="p-4">State</th></tr></thead><tbody>{workers.data.data.map((worker) => <tr key={worker.name} className="border-t border-zinc-800"><td className="p-4 font-mono text-xs text-zinc-200">{worker.name}</td><td className="p-4 text-zinc-400">{displayDate(worker.lastRunAt)}</td><td className="p-4 text-zinc-400">{displayDate(worker.lastSuccessAt)}</td><td className="p-4 text-zinc-400">{formatDuration(worker.intervalMs)}</td><td className="p-4"><Status worker={worker} /></td></tr>)}</tbody></table></div><p className="mt-3 text-xs text-zinc-500">Stale means no new tick started for more than two scheduled intervals (minimum 30 seconds). Flag-off workers are intentionally disabled; not registered means an enabled worker did not start and needs review.</p></section>;
}

function InboxView(): ReactElement {
  const [status, setStatus] = useConsoleUrlParam('inboxStatus'); const [search, setSearch] = useConsoleUrlParam('inboxSearch'); const [selected, setSelected] = useConsoleUrlParam('inboxId'); const [searchInput, setSearchInput] = useState(search); const [cursor, setCursor] = useState<string | null>(null); const [previousCursors, setPreviousCursors] = useState<Array<string | null>>([]); const [requeueCandidate, setRequeueCandidate] = useState<string | null>(null);
  const inbox = useQuery({ queryKey: ['inbox', status, search, cursor], queryFn: () => api<Page>(`/inbox?${new URLSearchParams({ limit: '25', ...(status ? { status } : {}), ...(search ? { search } : {}), ...(cursor ? { cursor } : {}) })}`), refetchInterval: 30_000 });
  const detail = useQuery({ queryKey: ['inbox', selected], queryFn: () => api<{ data: Row }>(`/inbox/${selected}`), enabled: Boolean(selected) });
  const client = useQueryClient();
  const requeue = useMutation({ mutationFn: (id: string) => api(`/inbox/${id}/requeue`, { method: 'POST' }), onSuccess: () => { void client.invalidateQueries({ queryKey: ['inbox'] }); setRequeueCandidate(null); setSelected(''); } });
  const resetPage = () => { setCursor(null); setPreviousCursors([]); setSelected(''); };
  if (inbox.isLoading) return <Loading title="Loading inbox…" />;
  if (inbox.isError) return <ErrorState message={(inbox.error as Error).message} />;
  if (!inbox.data) return <Loading title="Loading inbox…" />;
  return <section><PageTitle eyebrow="Read-first queue" title="Inbox" description="Searches safe metadata only. Select a row to view its full local detail." /><form onSubmit={(event) => { event.preventDefault(); setSearch(searchInput.trim()); resetPage(); }} className="mt-6 flex max-w-xl gap-2"><input value={searchInput} onChange={(event) => setSearchInput(event.target.value)} maxLength={100} placeholder="Search customer, sender, or subject" className="min-w-0 flex-1 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm outline-none ring-emerald-400 focus:ring-2" /><button className="rounded-lg bg-emerald-400 px-4 py-2 text-sm font-semibold text-zinc-950 hover:bg-emerald-300">Search</button></form><div className="mt-4 flex gap-2 overflow-x-auto">{['', 'pending', 'processing', 'processed', 'failed', 'skipped'].map((value) => <button key={value || 'all'} onClick={() => { setStatus(value); resetPage(); }} className={cn('rounded-full border px-3 py-1.5 text-sm capitalize', status === value ? 'border-emerald-400 bg-emerald-400 text-zinc-950' : 'border-zinc-700 text-zinc-300')}>{value || 'all'}</button>)}</div><div className="mt-6 grid gap-5 xl:grid-cols-[1fr_440px]"><Panel title={`${inbox.data.data.length} records`}><div className="divide-y divide-zinc-800">{inbox.data.data.length === 0 && <p className="py-4 text-sm text-zinc-500">No inbox records match these filters.</p>}{inbox.data.data.map((row) => <button key={String(row.id)} onClick={() => setSelected(String(row.id))} className="block w-full px-1 py-4 text-left hover:bg-zinc-900/50"><div className="flex items-center justify-between gap-3"><span className="font-medium">{display(row.customer_name)}</span><Badge value={display(row.status)} /></div><p className="mt-1 truncate text-sm text-zinc-400">{display(row.subject)}</p><p className="mt-2 text-xs text-zinc-500">{display(row.channel_name)} · {displayDate(row.received_at)}</p></button>)}</div><Pager page={previousCursors.length + 1} hasPrevious={previousCursors.length > 0} hasNext={Boolean(inbox.data.nextCursor)} onPrevious={() => { const previous = previousCursors[previousCursors.length - 1]; setPreviousCursors((items) => items.slice(0, -1)); setCursor(previous); setSelected(''); }} onNext={() => { if (inbox.data?.nextCursor) { setPreviousCursors((items) => [...items, cursor]); setCursor(inbox.data.nextCursor); setSelected(''); } }} /></Panel><Detail title="Inbox detail" loading={detail.isLoading} error={detail.error as Error | null} data={detail.data?.data} action={detail.data?.data.status === 'failed' ? <button onClick={() => setRequeueCandidate(String(detail.data?.data.id))} className="rounded-lg bg-amber-300 px-3 py-2 text-sm font-semibold text-zinc-950">Requeue failed item</button> : undefined} /></div>{requeueCandidate && <ConfirmActionDialog title="Requeue failed inbox item" description="This returns the failed item to pending processing and resets its retry count." confirmLabel="Requeue failed inbox item" pending={requeue.isPending} error={requeue.error as ApiError | null} onCancel={() => setRequeueCandidate(null)} onConfirm={() => requeue.mutate(requeueCandidate)} onRefresh={() => { void client.invalidateQueries({ queryKey: ['inbox'] }); void detail.refetch(); setRequeueCandidate(null); }} />}</section>;
}

function OutboundView(): ReactElement {
  const [status, setStatus] = useConsoleUrlParam('outboundStatus'); const [isDraft, setIsDraft] = useConsoleUrlParam('outboundDraft'); const [channel, setChannel] = useConsoleUrlParam('outboundChannel'); const [customer, setCustomer] = useConsoleUrlParam('outboundCustomer'); const [selected, setSelected] = useConsoleUrlOptionalParam('outboundId'); const [channelInput, setChannelInput] = useState(channel); const [customerInput, setCustomerInput] = useState(customer); const [cancelCandidate, setCancelCandidate] = useState<string | null>(null); const [cursor, setCursor] = useState<string | null>(null); const [previousCursors, setPreviousCursors] = useState<Array<string | null>>([]);
  const resetPage = () => { setCursor(null); setPreviousCursors([]); setSelected(null); };
  const outbound = useQuery({ queryKey: ['outbound', status, isDraft, channel, customer, cursor], queryFn: () => api<Page>(`/outbound?${new URLSearchParams({ limit: '25', ...(status ? { status } : {}), ...(isDraft ? { isDraft } : {}), ...(channel ? { channel } : {}), ...(customer ? { customer } : {}), ...(cursor ? { cursor } : {}) })}`), refetchInterval: 30_000 });
  const detail = useQuery({ queryKey: ['outbound', selected], queryFn: () => api<{ data: Row }>(`/outbound/${selected}`), enabled: Boolean(selected) });
  const client = useQueryClient();
  const cancel = useMutation({ mutationFn: (id: string) => api(`/outbound/${id}/cancel`, { method: 'POST' }), onSuccess: () => { void client.invalidateQueries({ queryKey: ['outbound'] }); setCancelCandidate(null); setSelected(null); } });
  if (outbound.isLoading) return <Loading title="Loading outbound queue…" />;
  if (outbound.isError) return <ErrorState message={(outbound.error as Error).message} />;
  if (!outbound.data) return <Loading title="Loading outbound queue…" />;
  return <section><PageTitle eyebrow="Delivery safety" title="Outbound queue" description="This console cannot send or resend messages. Cancellation is restricted to approved, unsent, non-draft rows." /><form onSubmit={(event) => { event.preventDefault(); setChannel(channelInput.trim()); setCustomer(customerInput.trim()); resetPage(); }} className="mt-6 grid max-w-3xl gap-2 sm:grid-cols-[1fr_1fr_auto]"><input value={channelInput} onChange={(event) => setChannelInput(event.target.value)} maxLength={100} placeholder="Filter channel" className="min-w-0 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm outline-none ring-emerald-400 focus:ring-2" /><input value={customerInput} onChange={(event) => setCustomerInput(event.target.value)} maxLength={100} placeholder="Filter customer" className="min-w-0 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm outline-none ring-emerald-400 focus:ring-2" /><button className="rounded-lg bg-emerald-400 px-4 py-2 text-sm font-semibold text-zinc-950 hover:bg-emerald-300">Filter</button></form><div className="mt-4 flex flex-wrap gap-2"><select value={isDraft} onChange={(event) => { setIsDraft(event.target.value); resetPage(); }} className="rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm"><option value="">All drafts</option><option value="true">Drafts</option><option value="false">Non-drafts</option></select></div><div className="mt-4 flex gap-2 overflow-x-auto">{['', 'pending', 'approved', 'sending', 'sent', 'failed', 'cancelled'].map((value) => <button key={value || 'all'} onClick={() => { setStatus(value); resetPage(); }} className={cn('rounded-full border px-3 py-1.5 text-sm capitalize', status === value ? 'border-emerald-400 bg-emerald-400 text-zinc-950' : 'border-zinc-700 text-zinc-300')}>{value || 'all'}</button>)}</div><div className="mt-6 grid gap-5 xl:grid-cols-[1fr_440px]"><Panel title={`${outbound.data.data.length} records`}><div className="divide-y divide-zinc-800">{outbound.data.data.length === 0 && <p className="py-4 text-sm text-zinc-500">No outbound records match these filters.</p>}{outbound.data.data.map((row) => <button key={String(row.id)} onClick={() => setSelected(String(row.id))} className="block w-full px-1 py-4 text-left hover:bg-zinc-900/50"><div className="flex items-center justify-between gap-3"><span className="font-medium">{display(row.customer_name)}</span><Badge value={display(row.status)} /></div><p className="mt-1 truncate text-sm text-zinc-400">{display(row.subject)}</p><p className="mt-2 text-xs text-zinc-500">{display(row.channel_name)} · {row.is_draft === true ? 'draft' : 'non-draft'} · {displayDate(row.created_at)}</p></button>)}</div><Pager page={previousCursors.length + 1} hasPrevious={previousCursors.length > 0} hasNext={Boolean(outbound.data.nextCursor)} onPrevious={() => { const previous = previousCursors[previousCursors.length - 1]; setPreviousCursors((items) => items.slice(0, -1)); setCursor(previous); setSelected(null); }} onNext={() => { if (outbound.data?.nextCursor) { setPreviousCursors((items) => [...items, cursor]); setCursor(outbound.data.nextCursor); setSelected(null); } }} /></Panel><Detail title="Outbound detail" loading={detail.isLoading} error={detail.error as Error | null} data={detail.data?.data} action={detail.data?.data.status === 'approved' && detail.data?.data.is_draft === false ? <button onClick={() => setCancelCandidate(String(detail.data?.data.id))} className="rounded-lg bg-red-300 px-3 py-2 text-sm font-semibold text-zinc-950">Cancel approved send</button> : undefined} /></div>{cancelCandidate && <ConfirmActionDialog title="Cancel approved outbound item" description="This cancels the approved non-draft send. It cannot be restored from the console." confirmLabel="Cancel approved send" danger pending={cancel.isPending} error={cancel.error as ApiError | null} onCancel={() => setCancelCandidate(null)} onConfirm={() => cancel.mutate(cancelCandidate)} onRefresh={() => { void client.invalidateQueries({ queryKey: ['outbound'] }); void detail.refetch(); setCancelCandidate(null); }} />}</section>;
}

function CustomersView(): ReactElement {
  const [search, setSearch] = useConsoleUrlParam('customerSearch'); const [selected, setSelected] = useConsoleUrlOptionalParam('customerId'); const [searchInput, setSearchInput] = useState(search); const [customerCursor, setCustomerCursor] = useState<string | null>(null); const [customerPrevious, setCustomerPrevious] = useState<Array<string | null>>([]); const [timelineCursor, setTimelineCursor] = useState<string | null>(null); const [timelinePrevious, setTimelinePrevious] = useState<Array<string | null>>([]);
  const customers = useQuery({ queryKey: ['customers', search, customerCursor], queryFn: () => api<Page>(`/customers?${new URLSearchParams({ limit: '25', ...(search ? { search } : {}), ...(customerCursor ? { cursor: customerCursor } : {}) })}`), refetchInterval: 30_000 });
  const detail = useQuery({ queryKey: ['customer', selected], queryFn: () => api<{ data: Row }>(`/customers/${selected}`), enabled: Boolean(selected) });
  const timeline = useQuery({ queryKey: ['customer-timeline', selected, timelineCursor], queryFn: () => api<Page>(`/customers/${selected}/timeline?${new URLSearchParams({ limit: '25', ...(timelineCursor ? { cursor: timelineCursor } : {}) })}`), enabled: Boolean(selected) });
  const resetCustomerPage = () => { setCustomerCursor(null); setCustomerPrevious([]); setSelected(null); setTimelineCursor(null); setTimelinePrevious([]); };
  const selectCustomer = (id: string) => { setSelected(id); setTimelineCursor(null); setTimelinePrevious([]); };
  if (customers.isLoading) return <Loading title="Loading local customers…" />;
  if (customers.isError) return <ErrorState message={(customers.error as Error).message} />;
  if (!customers.data) return <Loading title="Loading local customers…" />;
  return <section><PageTitle eyebrow="Local customer record" title="Customers" description="A bounded timeline built only from orchestrator records. It contains event metadata, never inbox bodies or agent output." />
    <form onSubmit={(event) => { event.preventDefault(); setSearch(searchInput.trim()); resetCustomerPage(); }} className="mt-6 flex max-w-xl gap-2"><input value={searchInput} onChange={(event) => setSearchInput(event.target.value)} maxLength={100} placeholder="Search customer name or BP reference" className="min-w-0 flex-1 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm outline-none ring-emerald-400 focus:ring-2" /><button className="rounded-lg bg-emerald-400 px-4 py-2 text-sm font-semibold text-zinc-950 hover:bg-emerald-300">Search</button></form>
    <div className="mt-6 grid gap-5 xl:grid-cols-[minmax(280px,0.8fr)_minmax(0,1.5fr)]"><Panel title={`${customers.data.data.length} local customers`}><div className="divide-y divide-zinc-800">{customers.data.data.length === 0 && <p className="py-4 text-sm text-zinc-500">No local customer records match this search.</p>}{customers.data.data.map((row) => <button key={String(row.id)} onClick={() => selectCustomer(String(row.id))} className={cn('block w-full px-1 py-4 text-left hover:bg-zinc-900/50', selected === String(row.id) && 'bg-zinc-800/60')}><div className="flex items-center justify-between gap-3"><span className="truncate font-medium">{display(row.display_name)}</span><span className="text-xs text-zinc-500">{String(row.inbox_count ?? 0)} inbox</span></div><p className="mt-1 text-xs text-zinc-500">{display(row.bp_ref)} · added {displayDate(row.created_at)}</p></button>)}</div><Pager page={customerPrevious.length + 1} hasPrevious={customerPrevious.length > 0} hasNext={Boolean(customers.data.nextCursor)} onPrevious={() => { const previous = customerPrevious[customerPrevious.length - 1]; setCustomerPrevious((items) => items.slice(0, -1)); setCustomerCursor(previous); setSelected(null); setTimelineCursor(null); setTimelinePrevious([]); }} onNext={() => { if (customers.data?.nextCursor) { setCustomerPrevious((items) => [...items, customerCursor]); setCustomerCursor(customers.data.nextCursor); setSelected(null); setTimelineCursor(null); setTimelinePrevious([]); } }} /></Panel>
      <div className="space-y-5"><Detail title="Customer record" loading={detail.isLoading} error={detail.error as Error | null} data={detail.data?.data} /><Panel title="Activity timeline">{!selected && <p className="text-sm text-zinc-500">Select a customer to see local activity.</p>}{timeline.isLoading && <p className="text-sm text-zinc-400">Loading timeline…</p>}{timeline.isError && <ErrorState message={(timeline.error as Error).message} />}{timeline.data && <div className="space-y-4">{timeline.data.data.length === 0 && <p className="text-sm text-zinc-500">No local activity recorded for this customer.</p>}{timeline.data.data.map((event) => <article key={`${String(event.event_type)}-${String(event.entity_id)}`} className="border-l border-zinc-700 pl-4"><div className="flex flex-wrap items-center gap-2"><Badge value={display(event.event_type)} /><span className="text-sm font-medium">{display(event.status)}</span></div><p className="mt-1 text-xs text-zinc-500">{displayDate(event.created_at)}</p>{typeof event.portal_task_url === 'string' && <a href={event.portal_task_url} target="_blank" rel="noreferrer" className="mt-2 inline-block text-xs font-medium text-emerald-300 hover:text-emerald-200">Open task in portal</a>}{Boolean(event.metadata) && <pre className="mt-2 max-h-32 overflow-auto whitespace-pre-wrap break-words rounded bg-zinc-950 p-2 text-xs text-zinc-400">{JSON.stringify(event.metadata, null, 2)}</pre>}</article>)}<Pager page={timelinePrevious.length + 1} hasPrevious={timelinePrevious.length > 0} hasNext={Boolean(timeline.data.nextCursor)} onPrevious={() => { const previous = timelinePrevious[timelinePrevious.length - 1]; setTimelinePrevious((items) => items.slice(0, -1)); setTimelineCursor(previous); }} onNext={() => { if (timeline.data?.nextCursor) { setTimelinePrevious((items) => [...items, timelineCursor]); setTimelineCursor(timeline.data.nextCursor); } }} /></div>}</Panel></div>
    </div>
  </section>;
}

function DecisionsView(): ReactElement {
  const [type, setType] = useConsoleUrlParam('decisionType'); const [outcome, setOutcome] = useConsoleUrlParam('decisionOutcome'); const [search, setSearch] = useConsoleUrlParam('decisionSearch'); const [selected, setSelected] = useConsoleUrlOptionalParam('decisionId'); const [searchInput, setSearchInput] = useState(search); const [cursor, setCursor] = useState<string | null>(null); const [previousCursors, setPreviousCursors] = useState<Array<string | null>>([]);
  const decisions = useQuery({ queryKey: ['decisions', type, outcome, search, cursor], queryFn: () => api<Page>(`/decisions?${new URLSearchParams({ limit: '25', ...(type ? { type } : {}), ...(outcome ? { outcome } : {}), ...(search ? { search } : {}), ...(cursor ? { cursor } : {}) })}`), refetchInterval: 30_000 });
  const detail = useQuery({ queryKey: ['decision', selected], queryFn: () => api<{ data: Row }>(`/decisions/${selected}`), enabled: Boolean(selected) });
  const resetPage = () => { setCursor(null); setPreviousCursors([]); setSelected(null); };
  if (decisions.isLoading) return <Loading title="Loading decisions…" />;
  if (decisions.isError) return <ErrorState message={(decisions.error as Error).message} />;
  if (!decisions.data) return <Loading title="Loading decisions…" />;
  return <section><PageTitle eyebrow="Human-in-the-loop" title="Decisions" description="Triage, draft, override, and backfill records with their local queue and task references. Resolution reflects decision state; Telegram callbacks are an approximation, not a complete delivery log." /><form onSubmit={(event) => { event.preventDefault(); setSearch(searchInput.trim()); resetPage(); }} className="mt-6 flex max-w-xl gap-2"><input value={searchInput} onChange={(event) => setSearchInput(event.target.value)} maxLength={100} placeholder="Search customer, type, or task reference" className="min-w-0 flex-1 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm outline-none ring-emerald-400 focus:ring-2" /><button className="rounded-lg bg-emerald-400 px-4 py-2 text-sm font-semibold text-zinc-950 hover:bg-emerald-300">Search</button></form><div className="mt-4 flex flex-wrap gap-2"><select value={type} onChange={(event) => { setType(event.target.value); resetPage(); }} className="rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm"><option value="">All types</option><option value="triage">triage</option><option value="draft_reply">draft reply</option><option value="backfill_task_proposal">backfill task proposal</option><option value="human_override">human override</option></select><select value={outcome} onChange={(event) => { setOutcome(event.target.value); resetPage(); }} className="rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm"><option value="">All outcomes</option>{['pending', 'accepted', 'modified', 'rejected', 'revised'].map((value) => <option key={value} value={value}>{value}</option>)}</select></div><div className="mt-6 grid gap-5 xl:grid-cols-[1fr_440px]"><Panel title={`${decisions.data.data.length} decisions`}><div className="divide-y divide-zinc-800">{decisions.data.data.length === 0 && <p className="py-4 text-sm text-zinc-500">No decisions match these filters.</p>}{decisions.data.data.map((row) => <button key={String(row.id)} onClick={() => setSelected(String(row.id))} className="block w-full px-1 py-4 text-left hover:bg-zinc-900/50"><div className="flex items-center justify-between"><span className="font-medium">{decisionLabel(row.decision_type)}</span><Badge value={display(row.outcome)} /></div><p className="mt-2 text-xs text-zinc-500">{display(row.customer_name)} · {displayDate(row.created_at)}</p><DecisionReferences row={row} /></button>)}</div><Pager page={previousCursors.length + 1} hasPrevious={previousCursors.length > 0} hasNext={Boolean(decisions.data.nextCursor)} onPrevious={() => { const previous = previousCursors[previousCursors.length - 1]; setPreviousCursors((items) => items.slice(0, -1)); setCursor(previous); setSelected(null); }} onNext={() => { if (decisions.data?.nextCursor) { setPreviousCursors((items) => [...items, cursor]); setCursor(decisions.data.nextCursor); setSelected(null); } }} /></Panel><Detail title="Decision detail" loading={detail.isLoading} error={detail.error as Error | null} data={detail.data?.data} /></div></section>;
}

function PageTitle({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }): ReactElement { return <div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-300">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-zinc-400">{description}</p></div>; }
function Panel({ title, children }: { title: string; children: ReactNode }): ReactElement { return <section className="rounded-xl border border-zinc-800 bg-zinc-900/40 p-5"><h2 className="mb-4 text-sm font-semibold">{title}</h2>{children}</section>; }
function Metric({ label, value, detail, tone, onClick }: { label: string; value: string; detail?: string; tone?: 'good' | 'bad'; onClick?: () => void }): ReactElement { return <button onClick={onClick} className={cn('rounded-xl border border-zinc-800 bg-zinc-900/40 p-5 text-left', onClick && 'hover:border-zinc-600')}><p className="text-sm text-zinc-400">{label}</p><p className={cn('mt-3 text-2xl font-semibold', tone === 'good' && 'text-emerald-300', tone === 'bad' && 'text-red-300')}>{value}</p>{detail && <p className="mt-2 text-xs text-zinc-500">{detail}</p>}</button>; }
function Badge({ value }: { value: string }): ReactElement { const bad = ['failed', 'rejected', 'cancelled'].includes(value); return <span className={cn('rounded-full px-2 py-1 text-xs font-medium', bad ? 'bg-red-400/15 text-red-300' : value === 'pending' ? 'bg-amber-400/15 text-amber-200' : 'bg-zinc-800 text-zinc-300')}>{value}</span>; }
function decisionLabel(value: unknown): string { return ({ triage: 'Triage', draft_reply: 'Draft review', human_override: 'Human override', backfill_task_proposal: 'Backfill proposal' } as Record<string, string>)[String(value)] ?? display(value); }
function DecisionReferences({ row }: { row: Row }): ReactElement { const refs = [row.inbox_message_id ? `inbox #${String(row.inbox_message_id)}` : null, row.outbound_queue_id ? `outbound #${String(row.outbound_queue_id)} (${display(row.outbound_status)})` : null, row.task_ref ? `task ${String(row.task_ref)}` : null].filter((value): value is string => Boolean(value)); return <div className="mt-2 flex flex-wrap gap-x-2 gap-y-1 text-xs text-zinc-500">{refs.map((ref) => <span key={ref}>{ref}</span>)}{typeof row.portal_task_url === 'string' && <a href={row.portal_task_url} target="_blank" rel="noreferrer" onClick={(event) => event.stopPropagation()} className="font-medium text-emerald-300 hover:text-emerald-200">Open task in portal</a>}</div>; }
function StateBadge({ enabled, onLabel = 'available', offLabel = 'off' }: { enabled: boolean; onLabel?: string; offLabel?: string }): ReactElement { return <span className={cn('shrink-0 rounded-full px-2 py-1 text-xs font-medium', enabled ? 'bg-emerald-400/15 text-emerald-300' : 'bg-zinc-800 text-zinc-400')}>{enabled ? onLabel : offLabel}</span>; }
function QueueStates({ label, rows }: { label: string; rows: QueueState[] }): ReactElement { return <div className="mb-4 last:mb-0"><p className="mb-2 text-xs font-medium uppercase tracking-wide text-zinc-500">{label}</p>{rows.length === 0 ? <p className="text-sm text-zinc-500">No queued records.</p> : <div className="flex flex-wrap gap-2">{rows.map((row) => <span key={row.status} className="rounded bg-zinc-950 px-2 py-1 text-xs text-zinc-300">{row.status}: {row.count}</span>)}</div>}</div>; }
function Pager({ page, hasPrevious, hasNext, onPrevious, onNext }: { page: number; hasPrevious: boolean; hasNext: boolean; onPrevious: () => void; onNext: () => void }): ReactElement { return <div className="mt-5 flex items-center justify-between border-t border-zinc-800 pt-4 text-sm"><span className="text-zinc-500">Page {page}</span><div className="flex gap-2"><button disabled={!hasPrevious} onClick={onPrevious} className="rounded border border-zinc-700 px-3 py-1.5 text-zinc-300 disabled:opacity-40">Previous</button><button disabled={!hasNext} onClick={onNext} className="rounded border border-zinc-700 px-3 py-1.5 text-zinc-300 disabled:opacity-40">Next</button></div></div>; }
function ConfirmActionDialog({ title, description, confirmLabel, danger = false, pending, error, onCancel, onConfirm, onRefresh }: { title: string; description: string; confirmLabel: string; danger?: boolean; pending: boolean; error: ApiError | null; onCancel: () => void; onConfirm: () => void; onRefresh: () => void }): ReactElement { const conflict = error?.status === 409; return <div role="dialog" aria-modal="true" aria-labelledby="confirm-action-title" className="fixed inset-0 z-30 grid place-items-center bg-black/70 p-5"><div className="w-full max-w-md rounded-xl border border-zinc-700 bg-zinc-950 p-6 shadow-2xl"><h2 id="confirm-action-title" className="text-lg font-semibold">{title}</h2><p className="mt-3 text-sm leading-6 text-zinc-400">{description}</p>{conflict ? <div className="mt-5 rounded-lg border border-amber-700/60 bg-amber-950/30 p-4 text-sm text-amber-100"><p className="font-medium">State changed elsewhere</p><p className="mt-1 text-amber-100/80">Refresh and review the current row before taking another action.</p><button onClick={onRefresh} className="mt-3 rounded-lg bg-amber-300 px-3 py-2 text-sm font-semibold text-zinc-950">Refresh row</button></div> : error ? <p className="mt-5 text-sm text-red-300">{error.message}</p> : null}<div className="mt-6 flex justify-end gap-3"><button disabled={pending} onClick={onCancel} className="rounded-lg border border-zinc-700 px-3 py-2 text-sm text-zinc-200 disabled:opacity-50">Keep unchanged</button><button disabled={pending || conflict} onClick={onConfirm} className={cn('rounded-lg px-3 py-2 text-sm font-semibold text-zinc-950 disabled:opacity-50', danger ? 'bg-red-300' : 'bg-amber-300')}>{pending ? 'Applying…' : confirmLabel}</button></div></div></div>; }
function formatDuration(ms: number): string { return ms >= 60_000 ? `${Math.round(ms / 60_000)}m` : `${Math.max(1, Math.round(ms / 1_000))}s`; }
function Status({ worker }: { worker: Worker }): ReactElement { const labels: Record<WorkerState, string> = { registered_idle: 'registered · idle', working: 'working', healthy: 'healthy', stale: 'stale', failing_backoff: `${worker.lastError ?? 'failed'} · backing off (${worker.consecutiveFailures})`, not_registered: worker.registration === 'flag_off' ? 'flag off' : 'not registered' }; const bad = worker.state === 'stale' || worker.state === 'failing_backoff' || worker.registration === 'not_registered'; const pending = worker.state === 'registered_idle' || worker.state === 'working' || worker.registration === 'flag_off'; return <span className={cn('inline-flex items-center gap-2', bad ? 'text-red-300' : pending ? 'text-amber-200' : 'text-emerald-300')}>{bad ? <CircleAlert size={16} /> : <CheckCircle2 size={16} />}{labels[worker.state]}</span>; }
function Attention({ workers }: { workers: Worker[] }): ReactElement { const needingAttention = workers.filter((worker) => worker.state === 'failing_backoff' || worker.state === 'stale' || worker.registration === 'not_registered'); return needingAttention.length ? <div className="space-y-3">{needingAttention.map((worker) => <div key={worker.name} className="flex items-center justify-between rounded-lg bg-red-400/10 p-3"><span className="font-mono text-xs">{worker.name}</span><Status worker={worker} /></div>)}</div> : <div className="flex items-center gap-3 rounded-lg bg-emerald-400/10 p-4 text-sm text-emerald-200"><CheckCircle2 size={18} />No workers need attention.</div>; }
function Loading({ title }: { title: string }): ReactElement { return <div className="mt-8 rounded-xl border border-zinc-800 p-8 text-sm text-zinc-400">{title}</div>; }
function ErrorState({ message }: { message: string }): ReactElement { return <div className="mt-8 flex items-center gap-3 rounded-xl border border-red-900/60 bg-red-950/30 p-5 text-sm text-red-200"><CircleAlert size={18} />{message}</div>; }
function Detail({ title, data, loading, error, action }: { title: string; data?: Row; loading: boolean; error: Error | null; action?: ReactNode }): ReactElement { return <Panel title={title}>{loading && <p className="text-sm text-zinc-400">Loading selected record…</p>}{error && <ErrorState message={error.message} />}{!loading && !error && !data && <p className="text-sm text-zinc-500">Select a row to reveal its detail.</p>}{data && <div className="space-y-4 text-sm">{Object.entries(data).map(([key, value]) => <div key={key}><p className="text-xs font-medium uppercase tracking-wide text-zinc-500">{key.replaceAll('_', ' ')}</p><pre className="mt-1 max-h-56 overflow-auto whitespace-pre-wrap break-words rounded bg-zinc-950 p-3 text-xs text-zinc-300">{typeof value === 'object' ? JSON.stringify(value, null, 2) : String(value ?? '—')}</pre></div>)}{action && <div className="border-t border-zinc-800 pt-4">{action}</div>}</div>}</Panel>; }
